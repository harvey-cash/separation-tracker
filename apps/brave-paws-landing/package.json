{
  "name": "brave-paws-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host=0.0.0.0 --port=3001",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\""
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}